name: Deploy BRV App to Oracle VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Copy files to VM and deploy
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VM_HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/brv-app || git clone https://github.com/Aarav500/brv-app.git && cd brv-app
            git pull origin main
            docker stop brv-app || true
            docker rm brv-app || true
            docker rmi brv-app || true
            docker build --no-cache -t brv-app .
            docker run -d --name brv-app -p 8501:8501 brv-app
